{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/oneclickoffice-demo/Dockerfile"
  },
  "deploy": {
    "watchPatterns": [
      "apps/oneclickoffice-demo/**",
      "packages/**",
      "package.json",
      "bun.lock",
      "bunfig.toml"
    ]
  }
}
